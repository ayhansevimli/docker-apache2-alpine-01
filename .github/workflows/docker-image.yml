name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name : docker login
      env: 
        DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
        DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      run: |
        docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_TOKEN

    - name: Set the value
      id: step_one
      run: |
        #echo "action_state=yellow" >> $GITHUB_ENV
        echo "action_state="$(date +%Y-%m-%d_%H-%M-%S) >> $GITHUB_ENV
    - name: Use the value
      id: step_two
      run: |
        echo "${{ env.action_state }}" # This will output 'yellow'

    - name: Build the Docker image
      run: |
       echo $(date +%Y-%m-%d_%H-%M-%S) > tagdate.txt
       cat tagdate.txt

       echo "------------------------------------"
       echo $(date +%Y-%m-%d_%H-%M-%S) > $tagdate2
       echo $tagdate2
       echo "------------------------------------"
       
       echo "This is the lead in sentence for the list" >> $GITHUB_STEP_SUMMARY
       echo $GITHUB_STEP_SUMMARY


       docker build . --file Dockerfile --tag ayhans/docker-image-apache2-alpine-01:latest
    - name: Push the Docker image
      run: |
        cat tagdate.txt
        echo $GITHUB_STEP_SUMMARY
        docker push ayhans/docker-image-apache2-alpine-01:$(date +%Y-%m-%d_%H-%M-%S)

